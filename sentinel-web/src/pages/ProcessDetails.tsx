import React from 'react';
import { Container, Typography } from '@mui/material';

const ProcessDetails: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Process Details</Typography>
    </Container>
  );
};

export default ProcessDetails;